# Confidential — environment, keys, credential dumps (templates stay tracked)
.env
.env.*
!.env.example
!.env.*.example

*.pem
*.p12
*.pfx
.secrets/
secrets/
mnemonic.txt
.secret

# Common cloud / wallet credential filenames
credentials.json
service-account*.json

node_modules/
dist/
.next/
out/
deployments.json
agent-ids.json
cache/
artifacts/
typechain-types/
*.local
.DS_Store
